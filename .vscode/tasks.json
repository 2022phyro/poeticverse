{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "echo Hello"
        },
        {
            "label": "server-dev",
            "type": "shell",
            "command": "flask --app 'api:create_app()' run --debug"
        },
        {
            "label": "celery-worker",
            "type": "shell",
            "command": "celery -A api.celery_worker worker --loglevel=info -P eventlet"
        },
        {
            "label": "celery-beat",
            "type": "shell",
            "command": "celery -A api.celery_worker beat"
        }
    ]
}
